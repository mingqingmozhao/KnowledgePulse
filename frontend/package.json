{
  "name": "knowledgepulse-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "build:mobile": "vue-tsc && vite build --mode mobile",
    "preview": "vite preview",
    "cap:sync": "npm run build && cap sync",
    "cap:android": "npm run build && cap sync android",
    "cap:android:mobile": "npm run build:mobile && cap sync android",
    "cap:open:android": "cap open android",
    "cap:run:android": "npm run build && cap run android",
    "cap:run:android:mobile": "npm run build:mobile && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@stomp/stompjs": "^7.0.0",
    "axios": "^1.6.0",
    "echarts": "^5.4.3",
    "element-plus": "^2.4.4",
    "pinia": "^2.1.7",
    "sockjs-client": "^1.6.1",
    "vditor": "^3.10.7",
    "vue": "^3.4.21",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.1",
    "@types/node": "^20.10.0",
    "@types/sockjs-client": "^1.5.4",
    "@vitejs/plugin-vue": "^5.0.2",
    "typescript": "^5.3.2",
    "unplugin-auto-import": "^21.0.0",
    "unplugin-vue-components": "^32.0.0",
    "vite": "^5.0.8",
    "vue-tsc": "^2.1.0"
  }
}
